import { Inter, Karla, Open_Sans, Plus_Jakarta_Sans, Spline_Sans } from "next/font/google";

export const inter = Inter({
	subsets: ["latin"],
	// variable: "--font-inter",
});

export const karla = Karla({
	subsets: ["latin"],
	variable: "--font-karla",
});

export const openSans = Plus_Jakarta_Sans({
	subsets: ["latin"],
	variable: "--font-body",
});
